import React from "react";

export default function Footer() {
    return (
        <footer className="h-8 w-screen flex items-center justify-center mt-auto bg-gray-100 ">
            <div className="w-80 flex justify-center">
                <h1>Footer (for social links, not content)</h1>
            </div>
        </footer>
    )
}